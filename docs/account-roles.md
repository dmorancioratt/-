# 账号与角色设计

## 设计目标

系统不再把所有用户都放在同一个后台视角里，而是拆成两个使用场景：

- 求职者/学生：维护个人画像，提交简历，查看能力差距、匹配结果、学习路径，并进入数字人面试练习。
- 企业 HR/管理者：维护岗位能力体系，查看候选人画像和简历数据，处理新岗位/新技能审核，评估系统解析与匹配效果。

## 数据模型

新增和扩展的数据表：

- `users`：账号、密码哈希、角色、展示名、邮箱、手机、组织、状态、创建时间
- `user_sessions`：登录会话 Token、过期时间
- `candidate_profiles`：候选人个人画像
- `resumes.user_id`：简历与候选人账号关联

密码使用 PBKDF2 哈希保存，不在数据库里保存明文密码。

## 角色权限

| 模块 | 求职者/学生 | 企业 HR/管理者 |
| --- | --- | --- |
| 系统概览 | 可看 | 可看 |
| 个人中心 | 可用 | 不显示 |
| 候选人管理 | 不可访问 | 可用 |
| 数据源管理 | 不显示 | 可用 |
| JD 解析 | 不显示 | 可用 |
| 岗位管理 | 不显示 | 可用 |
| 新岗位发现 | 不显示 | 可用 |
| 岗位能力更新 | 不显示 | 可用 |
| 能力图谱 | 可看 | 可看 |
| 简历解析 | 可用 | 可用 |
| 匹配分析 | 可用 | 可用 |
| 学习路径 | 可用 | 不显示 |
| 数字人面试官 | 可用 | 可用 |
| 人工审核 | 不显示 | 可用 |
| 测试评估 | 不显示 | 可用 |
| 系统设置 | 不显示 | 可用 |
| 账号设置 | 可用 | 可用 |

前端通过路由 `meta.roles` 控制菜单和访问跳转，后端通过 `require_roles()` 保护敏感接口。

## 个人画像

求职者个人画像由多个维度组成：

- 基础信息：真实姓名、学历、专业、学校
- 求职意向：目标岗位、意向城市、期望薪资
- 能力标签：技能选择或自定义输入
- 证书成果：证书、竞赛、奖项
- 经历材料：项目经历、实习经历
- 自我总结：个人优势、方向和补充信息

系统会根据画像字段完整度计算画像完成度。后续可进一步把这些信息与简历、面试记录和岗位图谱合并，交由已接入的 AI 服务生成更完整的能力画像。

## HR 候选人管理

企业 HR 可以查看：

- 候选人账号信息
- 画像完成度
- 目标岗位、学校、专业、城市、期望薪资
- 技能、证书、项目、实习、竞赛
- 候选人简历数量和最近简历

这部分为后续“候选人池”“面试记录”“推荐排序”“简历批注”继续扩展预留了数据边界。
